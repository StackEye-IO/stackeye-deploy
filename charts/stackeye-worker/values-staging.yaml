# Staging environment values for stackeye-worker

mode: single
replicaCount: 2

worker:
  region: "staging"
  concurrency: 10

config:
  logLevel: "info"

sealedSecrets:
  enabled: true
  encryptedData: {}
